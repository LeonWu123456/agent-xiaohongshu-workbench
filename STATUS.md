# Xiaoshimei Workbench Status

**Current role:** `REFERENCE_ONLY / OPTIONAL_EDITOR_SHELL`

**Primary production path:** `/xiaoshimei-visual-content` v2+

The Workbench repository and its test/evidence history are preserved. It is no longer a prerequisite or production authority for Xiaoshimei visual-content output. Stop default feature expansion.

Use Workbench only when a dedicated browser editor materially beats the host-native path for repeated direct manipulation, multi-user distribution, or a user explicitly requests the web editor. Otherwise use the Skill production kernel plus PPTX/Keynote or another capable host editor.

Do not delete historical logic maps, tests, deployments, or evidence. They remain useful prior art for renderer state, object editing, persistence, export readback, concurrency, rollback, and production identity.
