# 小师妹工作站双人体验测评

目标不是一起讨论“好不好看”，而是分别证明：一个真实选题能否从原文一路完成到可发布下载包，以及哪里阻碍了完成。

## 测试准备

- 地址：`http://127.0.0.1:4184/`
- 主测试者与实际使用者各做一次独立测试；使用同一个任务，但不要共用进行中的草稿。
- 每人开始前先回载同一份起始工作区备份，结束后分别导出自己的工作区备份和 ZIP。
- 第一次测试时，另一人不要在旁边提示；卡住就记录，不要代操作。
- 不上传 Cookie、Token、密码或无关隐私；当前站点没有公网鉴权，不要把 4184 暴露到公网。

## 唯一测试任务

用一段本人真实想发布的素材，完成一篇 2 页小红书图文：

1. 输入原文并生成文字。
2. 修改标题、正文和 Tags，直到本人愿意继续使用。
3. 生成 2 张图片；如需要，用参考图约束人物、动作或场景。
4. 在画布中修改至少一个文字图层、一个图片图层和底色/版式。
5. 保存，刷新页面并回载，确认修改仍在。
6. 下载 ZIP，检查图片、发布文案和清单是否完整。

## 每次必须记录

- 是否独立完成；若未完成，停在哪一步。
- 首个“可用草稿”耗时、最终下载包耗时。
- 图片生成次数、失败次数和不必要的付费重试次数。
- 第一个需要别人解释或帮助的地方。
- 最严重的一处“实际发生 / 原本预期”差异。
- 最想保留的一点、最想删掉或改变的一点。
- 7 天内是否愿意再次用于真实内容：是 / 否，以及原因。
- 最终工作区备份、ZIP；视觉问题再附截图。

## 修改优先级

- P0：无法完成、数据丢失、误扣费、导出错误。立即处理。
- P1：需要解释、绕路或反复重试。小师妹被阻断，或两人重复遇到时优先处理。
- P2：排版、文案、默认值等体验瑕疵。合并成一轮处理。
- P3：个人偏好。先记录，不覆盖另一人的有效习惯。

修复后只重跑同一任务和失败节点；不要用新增功能代替问题闭环。当前本地运行健康不等于小师妹已获得价值，她完成真实内容并愿意再次使用，才进入价值验证。

## 交回 Codex

把两份按 `feedback/FEEDBACK_TEMPLATE.md` 填写的记录、各自的工作区备份和 ZIP 拖入当前 Codex 任务。两位测试者的反馈分开提交，不先合并成共识。
