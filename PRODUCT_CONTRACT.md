# 小师妹 Studio｜产品真相合同

更新时间：2026-08-22

> 这是从跨对话需求、本地 Codex 历史、MeSy Task/Issue/评测和当前代码回读后收敛出的唯一产品合同。以后改版允许增强，不允许静默缩水。

## 1｜产品是什么

小师妹 Studio 是一个**本地优先、单用户、可编辑、有证据链的小红书图文内容工作台**。

它不是“一键爆款生成器”，也不是自动养号机器人。目标是把一个真实选题或原文，编译成可人工判断、可继续编辑、可下载发布、可回读现实结果的完整内容资产。

默认品牌/账号配置来自账号档案，不把“小师妹”某一套题材永久写死。人物是稳定视觉角色，内容主题可以持续扩展。

## 2｜唯一主流程

```text
研究/选题（可选）
→ 原文/主题
→ 只生成文字
→ 人工确认标题、正文、5 个 tags
→ 决定 1–8 页与画面上下文
→ 生成场景图片
→ 3:4 画布逐层编辑
→ 检查发布文案
→ 保存 / 回载 / 下载发布包
→ 人工发布
→ 24h / 72h / 7d 现实反馈
```

## 3｜文字生产合同

- 输入支持：直接写选题、粘贴原文、从“研究选题”带入候选。
- 先生成文字，**确认前图片调用数必须为 0**。
- 文字草稿必须同时展示：3 个实质不同标题候选、可编辑最终标题、完整发布正文、恰好 5 个可编辑 tags。
- 正文不能藏在导出包或图片背后。创作流收起后，标题、正文摘要和 tags 仍应在工作台可见。
- 用户修改任何文字后，后续配图必须基于最新确认稿，不得继续使用旧上下文。
- 文案质量 Gate 失败时显式报错，不用廉价钩子、无证据主张或模板套话凑 PASS。

## 4｜图片生产合同

- 当前生产主路优先使用火山方舟 / Seedream；架构保持 provider-neutral，不把 OpenAI 作为硬依赖。
- 图片必须与**当前选题、当前页语义、当前动作**相关，不能只因为人物一致就算成功。
- 小师妹身份参考属于长期角色资产；单次动作参考最多 3 张，只影响姿势、器械、场景等本轮动作信息。
- 身份参考与动作参考必须分开，动作图不能污染长期人物身份。
- 默认生成 1–8 页，AI 可以建议页数，用户可以覆盖。
- 当前页允许单独重新生成 **3 张候选图**，由用户选择，不自动覆盖当前图。
- 场景图以插画/生活化场景为主，不把大段中文直接烧进模型图片；最终中文由编辑层渲染。
- 图片预览必须适配屏幕，不得大到破坏工作区；最终发布尺寸保持 1080×1440（3:4）。

## 5｜编辑器合同

中央工作区必须始终保留 3:4 主画布，底部是页面胶片条，右侧是连续创作与对象编辑区，左侧只保留极简主导航。

每页至少有 6 类独立对象：`background / image / eyebrow / title / body / brand`。发布成品不显示页码。

编辑能力必须保留：

- 选择、显示/隐藏、锁定/解锁、前后层级；
- 文字内容、位置、宽度、字号、字重、行距、对齐、颜色；
- 图片替换、位置、画框宽高、横纵焦点、缩放；
- 页面排序、复制、删除；
- 撤销 / 重做；
- 模板/背景调整不得吞掉对象级编辑能力。

编辑器不是静态 HTML 截图器。保存后的设计数据必须可回载，重新打开后应继续编辑，而不是只剩 PNG。

### 5.1｜设计能力桥接合同

智能排版不是“页角色 + 模块数量 → 固定模板”。确认文字后的整组页面规划必须同时产出受约束的 `xiaoshimei.design-program.v1`：

- `composition`：本页是封面聚焦、连续编辑流、主角领读还是安静收束；
- `focal_order`：读者先看标题、主图、辅助信息还是细节；
- `rhythm / image_edge / image_scale`：行间节奏、首图方向与图像份量；
- `title_measure / whitespace_anchor`：标题行宽与主要留白落点；
- `hero_panel`：只引用既有 `content_role=hero`，不得篡改内容主次。

模型只拥有设计建议权，不拥有任意 HTML、CSS、DOM 或第二份画布状态。程序先经过枚举、范围、页面拓扑和产品不变量约束，再写入现有 `html_state`；当前 HTML 编辑器仍是唯一渲染与保存面。用户的改字、取景、模块位移、缩放、版式选择与 undo/redo 永远优先，并和设计程序一起回载、预览、导出。

旧稿没有设计程序时，按页面职责、真实文量、语义主角和结尾角色编译确定性回退；不得因为 schema 升级抹掉用户已经保存的版式或对象编辑。

## 6｜研究、账号与现实反馈

- “研究选题”负责找真实图文信号和候选方向，不直接发布，也不绕过文字确认门。
- 账号档案保存定位、人物与内容组合；题材与人物身份分离，允许人性关系、成长观察、东方生活/文化等方向继续扩展。
- 研究样本用于学习标题、首图、分页和互动机制；事实性主张仍应回到可靠来源。
- 保存的内容进入本地资产库，默认最多保留 50 份活跃资产，支持整库备份与恢复。
- 发布后反馈固定为 24h / 72h / 7d；缺失必须是 `UNKNOWN`，不能写成 0。
- 现实反馈只改变下一轮判断，不反向伪造“已经成功”。

## 7｜安全、发布与证据

- 默认本地 Beta，不自动发布小红书。
- 对外发布、账号权限扩大、付费、删除和不可逆动作继续经过人工 Gate。
- 真实生成必须有真实 provider 回执；服务失败时显式失败，不用演示图冒充 AI 成功。
- 生产包至少保留：PNG、标题/正文/tags、可回载 JSON、manifest、必要 Hash/尺寸/生成回执。
- “Build 成功”“页面能打开”“图片生成成功”都不能冒充现实内容效果。

## 8｜不可缩水清单

以下能力一旦已有，未来迁移、换 GitHub 基座或换 Provider 时不得无声明消失：两阶段文字→配图、完整正文可见、3 标题 + 5 tags、1–8 页、中央 3:4 画布、7 层编辑、图片替换与裁切、当前页 3 候选重绘、撤销重做、页面排序/复制/删除、保存回载、发布文案、发布包、资产库、账号档案、研究选题、现实反馈、Provider 状态与失败回执。

## 9｜当前物理拓扑

```text
Xiaoshimei-Studio-v2
  = 本地工作副本 / UI / 编辑器 / server / API

GitHub main
  = 唯一正式源码与版本权威

Vercel xiaoshimei-full-workbench
  = 正式在线运行入口；Preview 验收后提升同一制品

Projects/Workstreams/Xiaoshimei-Studio-v2
  = Workspace V2 稳定原件

Projects/Views/ByStatus/ACTIVE/Xiaoshimei-Studio-v2
  -> ../../../Workstreams/Xiaoshimei-Studio-v2

4184
  -> 直接服务 Xiaoshimei-Studio-v2 的 dist 与 API

4175
  -> 本地火山方舟生成 Provider

~/.mesy/runtime/packages/xiaoshimei-studio-v2
  -> 本地运行数据、生成图片与 Provider 回执；不进入 Git
```

旧 `Xiaoshimei-Studio` 已被取代。不得再建立第二套前端、Preview 产品、运行目录或控制面；回滚使用 Git commit 与 Vercel deployment，不靠复制一份旧工作台。

## 10｜本轮深化验收

1. 用户一眼看见当前处于“原文/文字/配图/排版/发布包”哪一步，以及唯一下一步。
2. 五个阶段可直接点击定位到对应区域，不用在右栏盲滚。
3. 创作流收起后，最终标题、正文摘要、5 tags 仍持续可见。
4. 上述改动不得破坏任何既有编辑、生成、保存、回载、导出、研究和现实反馈能力。
5. 桌面、移动端测试与真实浏览器截图通过后才算完成。

## 11｜小红书图文生产标准

封面、正文、KV/插图母版拆分、智能排版、自由编辑、导出和发布前现实验收，统一执行 [`logic/XHS_GRAPHIC_PRODUCTION_STANDARD.md`](logic/XHS_GRAPHIC_PRODUCTION_STANDARD.md)。对应规则绑定到生成区域、切片比例、页面结构、可编辑状态、导出几何与回归测试；不得只修当前一张图或只靠 CSS 覆写。
