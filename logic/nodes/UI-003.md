# UI-003｜当前文字的配图设置与生成

- 回答：用户看到、编辑或判断的界面对象是什么？
- 本节点答案：只控制当前已确认文字的配图参数和新操作；旧文字恢复任务不得冻结这里的页数、模式、参考图或提示词
- 上游：PAGE-001(exposes)
- 下游：ACT-003(triggers)
- 实现：`src/main.jsx`、`src/action-reference-media.mjs`、`src/media-asset-store.mjs`、`src/generation-feedback.mjs`
- 规则：以节点 ID、typed relation 和 confirmed draft / DraftRecord 身份为准；UI 标签不能自报完成。
- 验证：`tests/current-logic-map.test.mjs` 与对应消费者 Reality readback
