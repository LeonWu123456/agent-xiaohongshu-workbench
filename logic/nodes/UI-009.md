# UI-009｜旧配图任务恢复卡

- 回答：用户看到、编辑或判断的界面对象是什么？
- 本节点答案：把旧 confirmed draft 的 pending 操作独立显示；明确区分零模型调用分流、零付费发现和明确付费续步
- 上游：PAGE-001(exposes)、RULE-005(constrains)
- 下游：ACT-009(triggers)
- 实现：`src/main.jsx`、`src/styles.css`
- 规则：以节点 ID、typed relation 和 confirmed draft / DraftRecord 身份为准；UI 标签不能自报完成。
- 验证：`tests/current-logic-map.test.mjs` 与对应消费者 Reality readback

- 交互边界：`保留旧任务并解锁当前配图` 为零模型调用；`检查旧任务` 只 DISCOVER；任何 STEP 都必须另一次点击并明确标注付费。
