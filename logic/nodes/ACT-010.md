# ACT-010｜检查现有图片操作

- 回答：用户动作触发什么业务意图？
- 本节点答案：按现有 operation nonce 查询账本、checkpoint 与缓存；缺记录时可能调用文字模型重建分镜，不进入付费图片步骤。缺少本机原任务时进入资产库找回，不以新生成替代。
- 上游：UI-010(triggers)
- 下游：FLOW-006(routes_to)
- 实现：`src/main.jsx`、`src/public-image-run.mjs`
- 规则：图片上游调用数必须为 0；T=P、C≠T 时同时保留发布锁和画布修复出口。
- 验证：`tests/generation-feedback.test.mjs`、`tests/main-authority.test.mjs`、`tests/public-image-run.test.mjs`
