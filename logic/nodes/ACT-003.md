# ACT-003｜为当前文字生成图片

- 回答：用户或系统可以执行什么动作？
- 本节点答案：仅为当前 confirmed text 创建或续跑付费图片事务；若旧任务仍占 active holder，先零模型调用分流
- 上游：UI-003(triggers)
- 下游：FLOW-002(routes_to)
- 实现：`src/main.jsx`、`src/action-reference-media.mjs`、`src/media-asset-store.mjs`、`src/provider-client.mjs`
- 规则：以节点 ID、typed relation 和 confirmed draft / DraftRecord 身份为准；UI 标签不能自报完成。
- 验证：`tests/current-logic-map.test.mjs` 与对应消费者 Reality readback
