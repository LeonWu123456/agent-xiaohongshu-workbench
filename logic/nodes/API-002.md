# API-002｜浏览器本机持久化

- 回答：流程通过哪个明确接口读写外部或本机能力？
- 本节点答案：IndexedDB保存内容寻址媒体，新workspace v3 key只存引用，v2保持回滚前像
- 上游：FLOW-001(depends_on)、FLOW-002(depends_on)、FLOW-003(depends_on)、FLOW-004(depends_on)、FLOW-006(depends_on)、FLOW-005(depends_on)
- 下游：无（顶层能力入口）
- 实现：`src/workspace-state.mjs`、`src/generation-session.mjs`、`src/media-asset-store.mjs`
- 规则：以节点 ID、typed relation 和 confirmed draft / DraftRecord 身份为准；UI 标签不能自报完成。
- 验证：`tests/current-logic-map.test.mjs` 与对应消费者 Reality readback
