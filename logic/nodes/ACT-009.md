# ACT-009｜保留旧任务并解锁当前配图

- 回答：用户或系统可以执行什么动作？
- 本节点答案：在不调用 Provider 的前提下，将旧 pending 原子迁入 recovery sibling，并释放当前 DraftRecord 的 active holder
- 上游：UI-009(triggers)
- 下游：FLOW-006(routes_to)
- 实现：`src/main.jsx`、`src/workspace-state.mjs`
- 规则：以节点 ID、typed relation 和 confirmed draft / DraftRecord 身份为准；UI 标签不能自报完成。
- 验证：`tests/current-logic-map.test.mjs` 与对应消费者 Reality readback

- 原子性：同一 CAS 同时释放当前 DraftRecord 的 holder 并插入 recovery sibling；任一步冲突都保持源任务不变，Provider 调用必须为 0。
